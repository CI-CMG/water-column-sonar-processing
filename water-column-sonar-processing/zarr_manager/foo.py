# from src.water_column_sonar_processing.zarr.bar import Bar
from ..zarr_manager.bar import Bar


# from ...src.water_column_sonar_processing.zarr.bar import Bar


# def foo():
#     Bar().biz()

# def foo(url, method='GET', data=None, headers=None):
#     Bar().biz(url, method, data=data, headers=headers)

def foo():
    return Bar().biz()